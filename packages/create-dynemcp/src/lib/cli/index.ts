/**
 * DyneMCP CLI Module
 *
 * Exposes the main entrypoint for the create-dynemcp CLI, including the interactive project scaffolding logic.
 * Only the public API (run) is exported for use by the CLI entrypoint.
 */
export { run } from './core/cli.js'
